# --- generated artifacts (regenerated by npm install / gradlew) ---
node_modules/
build/
.gradle/
.idea/
*.iml
allure-results/
allure-report/
test-results/
playwright-report/
local.properties
*.log
.DS_Store

# --- provided by the company, not committed ---
# (their compiled assets — kept out to keep the repo lean and avoid publishing their binaries)
backend/images/*.tar
android/*.apk
